import React from 'react';

/** Android/Web: Sign in with Apple no aplica. */
export default function AppleSignInButton(_props: {
  disabled?: boolean;
  onBusyChange?: (busy: boolean) => void;
}) {
  return null;
}
